#include <stdio.h>
#include <iostream>
using namespace std;

int main()
{
    int a = 6, b=16;
    int count = 0;
    
    for(int i=a; i<b; i++){
      if(i % 2 == 0 && i % 3 == 0 ){
        count ++;    
      }
    }

    cout<<count;

    return 0;
}

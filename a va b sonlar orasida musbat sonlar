#include <stdio.h>
#include <iostream>
using namespace std;

int main()
{
    int a=-1, b = 10;
    int count = 0;
    
    for(int i=a; i<=b; i++){
      if(i > 0 ){
        count ++;    
      }
    }

    cout<<count;

    return 0;
}
